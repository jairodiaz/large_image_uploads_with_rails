<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Picture:</b>
  <%= image_tag(@user.picture_url, :width => 600) if @user.picture.present? %>
</p>

<p>
  <b>Picture:</b>
  <%= image_tag(@user.picture.thumb.url) if @user.picture.present? %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
